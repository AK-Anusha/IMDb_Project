spring.application.name=User_EurekaServer
server.port=8080
spring.application.name=user

#enable actuator related end points to true to prevent white-label error when clicking on server in eureka-port
management.endpoints.web.exposure.include=*